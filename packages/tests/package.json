{
  "name": "@open-bento/tests",
  "type": "module",
  "scripts": {
    "lint": "biome lint .",
    "format": "biome lint --write .",
    "test": "pnpm run /^test:/",
    "test:terraform": "terraform -chdir=src/terraform init"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@open-bento/config": "workspace:*"
  }
}